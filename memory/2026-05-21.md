# 2026-05-21 — Deep Dive: txnbankingdatamodel Repository

## Activity
Daniel requested deep dive into `https://github.com/danielssonn/txnbankingdatamodel` to identify architectural impacts.

## Repo Inventory (main branch, sha 796ef70)
**Total:** ~35 files across architecture spec + DDL implementation + docs

### Architecture.md (~29KB) — Full Spec
11 sections: canonical graph model, proof registry, agentic mandates, projections, Entity Platform API, ZKP integration, tech options (Neo4j+QLDB), key invariants, open questions. Phased 18-week plan.

### ddl/ — Databricks/Delta Lake Implementation (~30 SQL files)
- `_shared/` — types/enums, struct definitions, RLS functions
- `catalogs/` — catalog creation + grants (tb_canonical, tb_cb, tb_cm, tb_wm)  
- `tb_canonical/entity/` — nodes, edges, current views, KYC view, UBO resolution
- `tb_canonical/product/`, `/account/`, `/transaction/`, `/channel/` — domain schemas with append-only tables + ROW_NUMBER() materialized views
- `tb_canonical/proof/` — proof chains (append-only), proof records (hash-chained integrity), current chain view, broken chain detection
- `tb_canonical/metrics/` — 8 canonical KPI definitions as seed data + time-series fact table
- `pipelines/` — 4 Databricks Jobs: integrity sweep (15min), KPI computation (hourly), CB projection refresh (CDF-triggered), entitlement snapshot (event-driven)

### docs/Enagement.md
Stakeholder mapping matrix: Mandate → Authority → Participation → Gravity. Targets Head of GTB, COO, CDO, CRO, CTO/HAEA, Databricks Platform Owner, Chief AI Officer. Bootcamp framing.

## Key Findings — Architectural Impacts Identified and Delivered to Daniel
(See Telegram message for full analysis)
